package me.blubby.bmod.content.container;

import net.minecraft.world.SimpleContainer;

public class SecondEnderChestContainer extends SimpleContainer {

    public SecondEnderChestContainer(int size) {
        super(size); // Directly initialize SimpleContainer with the given size
    }
}
